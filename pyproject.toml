[project]
name = "darwin-router"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "numpy",
    "python-dotenv",
]

[tool.uv]
package = false
